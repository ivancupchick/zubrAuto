import { FieldNames as fieldNames } from '../../../../src/entities/FieldNames';
import { convertClientNumber as convertNumber } from '../../../../src/utils/number.utils';

export import FieldNames = fieldNames;
export const convertClientNumber = convertNumber;
