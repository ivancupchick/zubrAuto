import { FieldNames as fieldNames } from '../../../../src/temp/entities/FieldNames';
import { convertClientNumber as convertNumber } from '../../../../src/core/utils/number.utils';

export import FieldNames = fieldNames;
export const convertClientNumber = convertNumber;
